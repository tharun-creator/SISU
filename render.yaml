services:
  - type: web
    name: sisu-booking-backend
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "PYTHONPATH=backend uvicorn backend.main:app --host 0.0.0.0 --port $PORT"
